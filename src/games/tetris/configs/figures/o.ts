import { IFigureData } from '../../types';
export const configFigureO: IFigureData = {
    id: 'o',
    // prettier-ignore
    variants:[
 {cells: [ [0, 0], [1, 0], [0, 1], [1, 1],]}],
    indexMap: [0, 0, 0, 0],
};
